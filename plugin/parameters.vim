"{{{ source guard
let [s:plugin, s:enter] = maktaba#plugin#Enter(expand('<sfile>:p'))
if !s:enter
    finish
endif
"}}}

let g:rrom = 'rrom'
let g:uec = 'uec'
let g:enbc = 'enbc'
let g:cellc = 'cellc'
let g:tupc = 'tupc'
let g:mcec = 'mcec'
let g:lom = 'lom'
let g:common = 'common'
